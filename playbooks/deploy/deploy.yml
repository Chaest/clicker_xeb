---

- name: Deploy the app
  hosts: "{{ env_type }}_back_1"
  tasks:
    - name: Copy target to env
      copy:
        src: "../../target/"
        dest: "/usr/local/tomcat/webapps/"
      become: yes
